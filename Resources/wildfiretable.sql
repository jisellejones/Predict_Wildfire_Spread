
CREATE TABLE wildfire_data(
    fire_year INT,
    district VARCHAR,
    unit VARCHAR,
    fire_number VARCHAR,
    fire_name VARCHAR,
    legal VARCHAR,
    latitude VARCHAR,
    longitude VARCHAR,
    fuel_model VARCHAR,
    county VARCHAR,
    report_date DATE,
    general_cause VARCHAR,
    odf_acres DEC,
    total_acres DEC
);